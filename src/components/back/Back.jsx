import React from "react";

const Back = ({ title }) => {
  return (
    <>
      <section className="back">
        <h2>{title}</h2>
      </section>
      <div className="margin"></div>
    </>
  );
};

export default Back;
